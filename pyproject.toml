[project]
name = "llm-caller"
version = "0.1.0"
description = "A Python application for calling various LLM APIs (OpenAI and later Microsoft Foundry)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.7.2",
    "python-dotenv>=1.2.1",
]
